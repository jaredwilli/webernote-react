{
    // Full configuration list found here:
    // https://github.com/Microsoft/vscode-chrome-debug#other-optional-launch-config-fields
	"version": "0.2.0",
	"configurations": [
		{
			"type": "chrome",
			"request": "attach",
			"name": "Attach to Remote",
			"address": "localhost",
			"port": 9222,
			"webRoot": "${workspaceRoot}"
		}
	]
}
