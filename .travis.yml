# http://lint.travis-ci.org/
sudo: true
language: node_js
node_js:
    "stable"
cache:
    directories: node_modules
script:
    npm run build
    npm test
deploy:
    provider: surge
    project: ./build
    domain: webernote.surge.sh
    skip_cleanup: true
    on:
        master
        build-path
