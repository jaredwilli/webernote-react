import React from 'react';
import { shallow } from 'enzyme';

import FlexVerticalLayout from '../FlexVerticalLayout';

describe('FlexVerticalLayout component', () => {

    it('renders without crashing', () => {
        shallow(<Flex
VerticalLayout />);
    });
});
