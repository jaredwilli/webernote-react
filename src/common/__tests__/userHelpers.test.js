import React from 'react';
import assert from 'assert';

import { auth } from '../../data/firebase.js';
import { deepMerge } from '../helpers';
import * as utils from '../userHelpers';

describe('User Helper Functions', () => {

    it('creates new user object', () => {

    });
});
